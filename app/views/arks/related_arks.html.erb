<div class="container pt-5 pb-2 bg-dark">
  <%= render 'pages/navbar' %>
  <% flash.each do |type, msg| %>
    <div class="flash flash-<%= type %> alert alert-dismissible fade show" role="alert">
      <span><%= msg %></span>
      <a data-bs-dismiss="alert" aria-label="Close">
        <i class="fas fa-times"></i>
      </a>
    </div>
  <% end %>
    <h1 style="color:white">Dans le style</h1>
    <h2 style="color:white">de l'artiste</h2>
    <div class="artist" data-controller="caroussel">

      <% @artist_arks.first(3).each_with_index do |artist_ark, i| %>
        <input data-caroussel-target="input" type="radio" name="slider" id="input-<%= i + 1%>">
      <% end %>


      <div class="caroussel-artists">
        <% @artist_arks.first(3).each_with_index do |artist_ark, i| %>
          <label class="caroussel-artist" for="input-<%= i + 1%>" id="label-<%= i + 1 %>" data-action="click->caroussel#unableLink" data-ark-id="<%= artist_ark.id %>" data-caroussel-target="arks">
            <img src=<%= artist_ark.image_url %>>
          </label>
        <% end %>
      </div>
    </div>
  </div>
  <br>
  <div>
<div class="container">
  <h1>Dans le style</h1>
  <h2>de l'époque</h2>
    <div class = "related_cards">
    <% @date_arks.first(6).each do | date_ark | %>
      <%= link_to ark_path(date_ark), class: "related_card_category" do %>
        <%= image_tag date_ark.image_url %>
        <div class="truncate">
        <span style="color:black"><%= date_ark.name %></span> <br>
        <span style="color:black" class= "artist_name"><%= date_ark.artist.name %></span>
        </div>
      <% end %>
    <% end %>
    </div>
</div>
